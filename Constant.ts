const colors: ColorType = {
  shadowColor: '#52006A',
  baseColor: '#1abebe',
  pieColor1: '#fbd203',
  pieColor2: '#ff3c00',
};

export default colors;

interface ColorType {
  shadowColor: string;
  baseColor: string;
  pieColor1: string;
  pieColor2: string;
}
